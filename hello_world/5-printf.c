#include <stdio.h>

/**
 * main - Entry point
 * Description: prints a string
 * Return: Always 0
 */

int main(void)
{
printf "%s" "with proper grammer, but the outcome is a piece of art,";
return (0);
}
