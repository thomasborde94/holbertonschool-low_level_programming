#include "main.h"

/**
 * main - check the code
 * Description: check the code
 * Return: always 0
 */

int print_alphabet(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
